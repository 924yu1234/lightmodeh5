import React from 'react';

import IconAllChain from '../Icons/allChain';

export default function AllNetworkIcon({ size = 24 }: { size?: number }) {
  return <IconAllChain size={size} className="all-chain-icon" />;
}
